[package]
name = "doom-host"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
env_logger = { workspace = true }
polkavm = { path = "../../crates/polkavm" }
sdl2 = { workspace = true }

[features]
default = ["sdl2/bundled"]
